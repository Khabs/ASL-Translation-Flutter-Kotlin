import 'package:flutter/material.dart';

//UI for text to  sign screen

class TextToSignScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Text to Sign Screen'),
    );
  }
}