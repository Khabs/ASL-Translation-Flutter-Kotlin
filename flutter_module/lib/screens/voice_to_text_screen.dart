import 'package:flutter/material.dart';
//UI for voice to text screen
class VoiceToTextScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Voice to Text Screen'),
    );
  }
}